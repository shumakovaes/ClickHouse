SELECT timeSeriesKPSSTestState('trend', 8, 1000000)(toUInt64(number), toFloat64(cityHash64(number) % 1000003)) FROM numbers(1000) GROUP BY number % 1 ORDER BY number % 1 SETTINGS enable_time_series_aggregate_functions = 1, max_threads = 1 FORMAT RowBinary
