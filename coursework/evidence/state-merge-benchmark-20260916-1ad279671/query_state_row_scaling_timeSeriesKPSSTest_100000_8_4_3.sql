SELECT timeSeriesKPSSTestState('trend', 8, 1000000)(toUInt64(number), toFloat64(cityHash64(number) % 1000003)) FROM numbers(100000) GROUP BY number % 4 ORDER BY number % 4 SETTINGS enable_time_series_aggregate_functions = 1, max_threads = 1 FORMAT RowBinary
