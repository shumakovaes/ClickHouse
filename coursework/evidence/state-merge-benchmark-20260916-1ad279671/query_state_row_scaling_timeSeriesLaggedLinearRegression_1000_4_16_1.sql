SELECT timeSeriesLaggedLinearRegressionState(4, 1000000)(toUInt64(number), toFloat64(cityHash64(number) % 1000003) / 1000003.0) FROM numbers(1000) GROUP BY number % 16 ORDER BY number % 16 SETTINGS enable_time_series_aggregate_functions = 1, max_threads = 1 FORMAT RowBinary
